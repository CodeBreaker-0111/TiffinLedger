import {NextResponse}from"next/server";import{getSession}from"@/lib/auth";import{prisma}from"@/lib/prisma";export async function GET(){const s=await getSession();if(!s||s.role!=="CUSTOMER")return NextResponse.json({error:"Unauthorized"},{status:401});const c=await prisma.customer.findUnique({where:{accountUserId:s.userId},select:{id:true,name:true,phone:true,email:true,planPrice:true,status:true}});if(!c)return NextResponse.json({error:"Customer profile not found."},{status:404});return NextResponse.json({customer:c})}
