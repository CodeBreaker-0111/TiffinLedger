import {NextResponse}from"next/server";import {getSession}from"@/lib/auth";import {prisma}from"@/lib/prisma";import {getBill}from"@/lib/billing";export async function GET(req:Request,{params}:{params:Promise<{id:string}>}){const s=await getSession();if(!s)return NextResponse.json({error:"Unauthorized"},{status:401});const{id}=await params;const c=await prisma.customer.findFirst({where:{id,...(s.role==="OWNER"?{ownerId:s.userId}:{accountUserId:s.userId})}});if(!c)return NextResponse.json({error:"Customer not found."},{status:404});const month=new URL(req.url).searchParams.get("month")||new Date().toISOString().slice(0,7);try{return NextResponse.json(await getBill(id,month))}catch{return NextResponse.json({error:"Month must be YYYY-MM."},{status:400})}}
